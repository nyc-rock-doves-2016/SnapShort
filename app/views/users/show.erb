<h2>Hey there, <%= @user.username %>!</h2>

<% @user.prompts.each do |prompt| %>
  <div class="prompt-container">
	  <a class="prompt-link" href="/prompts/<%= prompt.id %>"><%= prompt.title %></a><br>
	</div>
<% end %>

<% @user.shorts.each do |short| %>
	<div class="short-container"
  <%= short.body %>
  <a class="create-link" href="/shorts/<%= short.id %>/comments">Add Comment</a></div>
<% end %>